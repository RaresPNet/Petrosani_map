{
  "name": "harta_petrosani",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler d1 execute petrosani --remote --file=./db/schema.sql && wrangler pages deploy ./public",
    "seed:local": "wrangler d1 execute petrosani --local --file=./db/seed.sql",
    "seed:remote": "wrangler d1 execute petrosani --remote --file=./db/seed.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
