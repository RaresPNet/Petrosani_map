{
  "name": "harta_petrosani",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public",
    "db:reset:local": "wrangler d1 execute petrosani --local --file=./db/schema.sql && wrangler d1 execute petrosani --local --file=./db/seed.sql",
    "db:reset:remote": "wrangler d1 execute petrosani --remote --file=./db/schema.sql && wrangler d1 execute petrosani --remote --file=./db/seed.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
